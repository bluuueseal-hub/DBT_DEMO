
version: 2

models:
    - name: test
      description: "test model select * from demo bike"
      columns:
          - name: RIDE_ID
            description: "The primary key for this table"
            tests:
                - unique
                - not_null


